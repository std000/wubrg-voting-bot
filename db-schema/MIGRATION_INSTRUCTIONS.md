# Инструкции по миграции базы данных

## Добавление поля emoji в poll_options

Эта миграция добавляет колонку `emoji` в таблицу `voting.poll_options`, что позволяет каждому варианту голосования иметь свой эмодзи для визуализации результатов.

### Применение миграции

Если у вас уже есть работающая база данных, выполните следующую команду:

```bash
psql -U your_username -d your_database -f db-schema/add_emoji_column.sql
```

Или подключитесь к вашей БД и выполните:

```sql
-- Добавляем колонку emoji (nullable, без значения по умолчанию)
ALTER TABLE voting.poll_options 
ADD COLUMN IF NOT EXISTS emoji TEXT;

-- Добавляем комментарий к колонке
COMMENT ON COLUMN voting.poll_options.emoji IS 'Эмодзи для визуализации голосов за этот вариант (nullable, по умолчанию 👍 в коде)';
```

### Для новых установок

Если вы создаете базу данных с нуля, просто используйте обновленный файл `schema.sql`:

```bash
psql -U your_username -d your_database -f db-schema/schema.sql
```

### Изменения в коде

После применения миграции бот автоматически начнет использовать эмодзи из базы данных при отображении результатов голосования. Каждый вариант теперь будет показываться с соответствующим ему эмодзи.

### Пример использования

По умолчанию все варианты с NULL или пустым emoji будут отображаться с 👍. Вы можете установить эмодзи для конкретного варианта, обновив значение в базе данных:

```sql
-- Изменить эмодзи для конкретного варианта
UPDATE voting.poll_options 
SET emoji = '⚪' 
WHERE id = 1;
```

### Примеры эмодзи для разных тем

- Цвета Magic: ⚪ 🔵 ⚫ 🔴 🟢
- События: 📅 🎉 🎮 ☀️ 🌙
- Оценки: 👍 👎 ⭐ ❤️ 🔥
- Еда: 🍕 🍔 🍜 🍰 🍺

